{
  "name": "herald",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '@herald/frontend' build",
    "dev": "bun run --filter '@herald/frontend' dev & bun packages/backend/dev.ts"
  }
}
