{
  "name": "herald",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "build": "bun run --filter '@herald/frontend' build",
    "dev": "bun --watch packages/backend/src/index.ts"
  }
}
